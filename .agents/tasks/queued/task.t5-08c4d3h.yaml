id: task.t5-08c4d3h
kind: task
version: 1
title: make authority bridge discovery event-driven
status: ready
priority: medium
design_ref:
  doc: docs/raw-pty-tunnel-design.md
  why:
    - ingress raw input skips bridge refresh, but steady-state ingress still scans authority sockets on a fixed interval
    - SSH parity requires removing periodic background work that can compete with raw input/output under many sessions
source_of_truth:
  file: .agents/tasks/backlog.yaml
  reference: next
depends_on:
  - task.t5-08c4d3g.done
linked_primitives:
  - primitive.verification-refresh
  - primitive.task-history-update
  - primitive.task-board-sync
done_criteria:
  - authority socket registration and unregistration flow through explicit lifecycle events
  - ingress no longer scans the temp directory on a fixed steady-state interval for healthy bridges
  - stale bridge cleanup still works on disconnect, process death, and authority-host restart
  - raw input and output routing does not compete with bridge discovery work
verification:
  - cargo test remote_node_ingress_server_runtime
  - manual: multi-session cross-host interaction while monitoring bridge refresh activity
touched_paths:
  - src/runtime/remote_node_ingress_server_runtime.rs
  - src/runtime/remote_main_slot_ingress_runtime.rs
  - src/runtime/remote_authority_target_host_runtime.rs
  - docs/raw-pty-tunnel-design.md
  - .agents/tasks/backlog.yaml
notes:
  - Do not replace fixed interval scans with another hidden polling loop.
